describe('{{ titleCase apiModuleName }} API Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});

describe('{{ titleCase apiModuleName }} Integration Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});