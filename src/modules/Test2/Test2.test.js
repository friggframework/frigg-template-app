describe('Test2 API Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});

describe('Test2 Integration Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});