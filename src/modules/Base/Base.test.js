describe('Base API Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});

describe('Base Integration Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});