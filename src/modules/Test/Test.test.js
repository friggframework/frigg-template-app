describe('Test API Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});

describe('Test Integration Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});