describe('Test1 API Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});

describe('Test1 Integration Tests', () => {
    it('should work', async () => {
        expect(1 + 1).toEqual(2);
    });
});