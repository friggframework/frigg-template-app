import { Filter } from '../components/Integration';

export default {
  component: Filter,
  title: 'Design System/Categories',
};

export const Default = {
  decorators: [
    (story) => story(),
  ],
};
