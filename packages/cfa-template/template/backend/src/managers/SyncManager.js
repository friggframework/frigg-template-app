const LHSyncManager = require('@friggframework/core/managers/LHSyncManager');

class SyncManager extends LHSyncManager {
    constructor(params) {
        super(params);
    }
}

module.exports = SyncManager;
